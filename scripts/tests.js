QUnit.test("FIXME: Select bar (selected and another).", function (assert) {
    let FIXME = null;

    assert.notEqual(FIXME, null);
});

QUnit.test("FIXME: Delete bar.", function (assert) {
    let FIXME = null;

    assert.notEqual(FIXME, null);
});

QUnit.test("FIXME: Modify bar.", function (assert) {
    let FIXME = null;

    assert.notEqual(FIXME, null);
});

QUnit.test("FIXME: Add bar.", function (assert) {
    let FIXME = null;

    assert.notEqual(FIXME, null);
});